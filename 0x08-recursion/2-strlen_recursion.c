#include "main.h"

/**
 * _strlen_recursion - returns the length of s string
 *  @s: a string
 *  Return: length
 */
int _strlen_recursion(char *s)
{
	int len = 0;

	if (*s == '\0')
		return
	_strlen_recursion(s);
	len++;
	return (*s);

}
